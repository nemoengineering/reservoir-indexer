import { ChainIdToAddress, resolveAddress } from "../utils";

export const PairFactory: ChainIdToAddress = resolveAddress("SudoswapV2", "PairFactory");

export const VeryFastRouter: ChainIdToAddress = resolveAddress("SudoswapV2", "VeryFastRouter");

export const Router: ChainIdToAddress = resolveAddress("SudoswapV2", "Router");
