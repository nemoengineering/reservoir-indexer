import { ChainIdToAddress, resolveAddress } from "../utils";

export const Collection: ChainIdToAddress = resolveAddress("ArtBlocks", "Collection");

export const MinterFilter: ChainIdToAddress = resolveAddress("ArtBlocks", "MinterFilter");
