import { ChainIdToAddress, resolveAddress } from "../utils";

export const Exchange: ChainIdToAddress = resolveAddress("BlurV2", "Exchange");

export const Delegate: ChainIdToAddress = resolveAddress("BlurV2", "Delegate");
